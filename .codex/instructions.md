## Next.js Initialization

When starting work on a Next.js project, **always call the `init` tool from the `next-devtools-mcp` server first**. This sets the proper context and ensures all subsequent Next.js queries use the official documentation (via `nextjs_docs`). Do this before any other project actions.
